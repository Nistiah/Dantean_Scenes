#include <stdio.h>
    int main() {
        int a, b, d,e;
        printf("Podaj 1 liczbe caĹkowita:");
        d=scanf("%d", &a);
        if(d==1)
        {
            printf("Podaj 2 liczbe caĹkowitÄ:");
            e = scanf("%d", &b);
        }
        if (d == 1&&e==1) {
            if (b != 0){
                if (a % b == 0) {
                    printf("%d is divisible by %d", a, b);
                }else
                {
                    printf("%d is not divisible by %d", a, b);
                }
            }else
            {
                printf("Operation not permitted");
                return 1;
            }
        }else
        {
            printf("Incorrect input");
            return 1;
        }
        return 0;
    }


